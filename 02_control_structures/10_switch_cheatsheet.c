// Switch case default的應用
switch (變數) {
    case 值1:
        // 當變數 == 值1 時執行
        break;
    case 值2:
        // 當變數 == 值2 時執行
        break;
    case 值3:
        // 當變數 == 值3 時執行
        break;
    default:
        // 當變數不符合上面任何 case 時執行
        break;
}
