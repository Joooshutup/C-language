// 99乘法表直式排列
#include <stdio.h>

int main() {    
    for (int i = 1; i <= 9; i++) {  // 控制乘數 (1~9)
        for (int j = 1; j <= 9; j++) {  // 控制被乘數 (1~9)
            printf("%d * %d = %2d  ", j, i, j * i);
        }
        printf("\n");  // 換行
    }
    return 0;
}
